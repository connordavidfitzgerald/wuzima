# Vercel's builder loses pnpm's symlinked node_modules layout, which left
# transitive deps like tslib (via rxjs, via sanity) unresolvable at bundle time.
# A flat, symlink-free install costs some disk and fixes it.
node-linker=hoisted
